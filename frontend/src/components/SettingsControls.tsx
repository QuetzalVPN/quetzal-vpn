import Button from './Button';

export default () => (
  <div className="flex ">
    <div className="ml-auto flex gap-4">
      <Button>Apply</Button>
      <Button>Discard</Button>
    </div>
  </div>
);
